<Window x:Class="Pet_Shop_Project.Views.ChangePasswordDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Đổi mật khẩu" 
        Height="520" 
        Width="500"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize">

    <Border Background="White"
            CornerRadius="20"
            BorderThickness="0">
        <Border.Effect>
            <DropShadowEffect ShadowDepth="0" BlurRadius="30" Opacity="0.3"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" 
                    Background="#FF6B6B"
                    CornerRadius="20,20,0,0"
                    Padding="30,20"
                    Margin="0,0,0,20">
                <Grid>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <materialDesign:PackIcon Kind="LockReset"
                                               Width="32"
                                               Height="32"
                                               Foreground="White"
                                               VerticalAlignment="Center"
                                               Margin="0,0,10,0"/>
                        <TextBlock Text="ĐỔI MẬT KHẨU"
                                  FontSize="24"
                                  FontWeight="Bold"
                                  Foreground="White"
                                  VerticalAlignment="Center"/>
                    </StackPanel>

                    
                </Grid>
            </Border>

            <!-- Content -->
            <ScrollViewer Grid.Row="1" 
                         VerticalScrollBarVisibility="Auto"
                         Padding="30,20">
                <StackPanel>

                    <!-- Old Password -->
                    <StackPanel Margin="0,0,0,20">
                        <TextBlock Text="Mật khẩu hiện tại"
                                  FontSize="14"
                                  FontWeight="SemiBold"
                                  Foreground="#666666"
                                  Margin="0,0,0,8" Width="400"/>
                        <Border Background="#F8FFFE"
                               BorderBrush="#E0E0E0"
                               BorderThickness="1"
                               CornerRadius="10" Width="408">
                            <Grid>
                                <PasswordBox x:Name="OldPasswordBox"
                                           FontSize="15"
                                           Padding="15,12"
                                           BorderThickness="0"
                                           Background="Transparent"
                                           PasswordChanged="PasswordBox_PasswordChanged"/>
                                <materialDesign:PackIcon Kind="Lock"
                                                       Width="20"
                                                       Height="20"
                                                       Foreground="#FF6B6B"
                                                       HorizontalAlignment="Right"
                                                       VerticalAlignment="Center"
                                                       Margin="0,0,15,0"
                                                       IsHitTestVisible="False"/>
                            </Grid>
                        </Border>
                        <TextBlock x:Name="OldPasswordError"
                                  Text=""
                                  FontSize="12"
                                  Foreground="#FF6B6B"
                                  Margin="5,5,0,0"
                                  Visibility="Collapsed"/>
                    </StackPanel>

                    <!-- New Password -->
                    <StackPanel Margin="0,0,0,20">
                        <TextBlock Text="Mật khẩu mới"
                                  FontSize="14"
                                  FontWeight="SemiBold"
                                  Foreground="#666666"
                                  Margin="0,0,0,8" Width="400"/>
                        <Border Background="#F8FFFE"
                               BorderBrush="#E0E0E0"
                               BorderThickness="1"
                               CornerRadius="10" Width="408">
                            <Grid>
                                <PasswordBox x:Name="NewPasswordBox"
                                           FontSize="15"
                                           Padding="15,12"
                                           BorderThickness="0"
                                           Background="Transparent"
                                           Width="408"
                                           PasswordChanged="PasswordBox_PasswordChanged"/>
                                <materialDesign:PackIcon Kind="LockOutline"
                                                       Width="20"
                                                       Height="20"
                                                       Foreground="#FF6B6B"
                                                       HorizontalAlignment="Right"
                                                       VerticalAlignment="Center"
                                                       Margin="0,0,15,0"
                                                       IsHitTestVisible="False"/>
                            </Grid>
                        </Border>
                        <TextBlock x:Name="NewPasswordError"
                                  Text=""
                                  FontSize="12"
                                  Foreground="#FF6B6B"
                                  Margin="5,5,0,0"
                                  Visibility="Collapsed"/>

                        <!-- Password Strength Indicator -->
                        <StackPanel x:Name="PasswordStrengthPanel" 
                                   Margin="5,8,0,0"
                                   Visibility="Collapsed">
                            <TextBlock Text="Độ mạnh mật khẩu:"
                                      FontSize="12"
                                      Foreground="#666666"
                                       Width="400"
                                      Margin="0,0,0,5"/>
                            <ProgressBar x:Name="PasswordStrengthBar"
                                        Height="4"
                                        Minimum="0"
                                        Maximum="100"
                                        Value="0"
                                        Width="400"
                                        Foreground="#4CAF50"/>
                            <TextBlock x:Name="PasswordStrengthText"
                                      FontSize="11"
                                      Width="400"
                                      Foreground="#666666"
                                      Margin="0,3,0,0"/>
                        </StackPanel>
                    </StackPanel>

                    <!-- Confirm Password -->
                    <StackPanel Margin="0,0,0,15">
                        <TextBlock Text="Xác nhận mật khẩu mới"
                                  FontSize="14"
                                  Width="400"
                                  FontWeight="SemiBold"
                                  Foreground="#666666"
                                  Margin="0,0,0,8"/>
                        <Border Background="#F8FFFE"
                               BorderBrush="#E0E0E0"
                               BorderThickness="1"
                               CornerRadius="10" Width="408">
                            <Grid>
                                <PasswordBox x:Name="ConfirmPasswordBox"
                                           FontSize="15"
                                           Padding="15,12"
                                           BorderThickness="0"
                                           Background="Transparent"
                                            Width="408"
                                           PasswordChanged="PasswordBox_PasswordChanged"/>
                                <materialDesign:PackIcon Kind="LockCheck"
                                                       Width="20"
                                                       Height="20"
                                                       Foreground="#FF6B6B"
                                                       HorizontalAlignment="Right"
                                                       VerticalAlignment="Center"
                                                       Margin="0,0,15,0"
                                                       IsHitTestVisible="False"/>
                            </Grid>
                        </Border>
                        <TextBlock x:Name="ConfirmPasswordError"
                                  Text=""
                                  FontSize="12"
                                  Foreground="#FF6B6B"
                                  Margin="5,5,0,0"
                                  Width="400"
                                  Visibility="Collapsed"/>
                    </StackPanel>

                    

                </StackPanel>
            </ScrollViewer>

            <!-- Footer Buttons -->
            <Border Grid.Row="2"
                    BorderBrush="#E0E0E0"
                    BorderThickness="0,0,0,0"
                    Padding="30,20">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Cancel Button -->
                    <Button Grid.Column="0"
                           Content="Hủy"
                           FontSize="14"
                           FontWeight="SemiBold"
                           Height="45"
                           Margin="0,0,10,0"
                           Background="#E0E0E0"
                           Foreground="#666666"
                           BorderThickness="0"
                           Cursor="Hand"
                           Click="CancelButton_Click">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}"
                                                   CornerRadius="22.5">
                                                <ContentPresenter HorizontalAlignment="Center"
                                                                VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#BDBDBD"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>

                    <!-- Save Button -->
                    <Button Grid.Column="1"
                           x:Name="SaveButton"
                           Content="Lưu thay đổi"
                           FontSize="14"
                           FontWeight="SemiBold"
                           Height="45"
                           Margin="10,0,0,0"
                           Background="#FF6B6B"
                           Foreground="White"
                           BorderThickness="0"
                           Cursor="Hand"
                           Click="SaveButton_Click">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}"
                                                   CornerRadius="22.5">
                                                <ContentPresenter HorizontalAlignment="Center"
                                                                VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#F56B5B"/>
                                    </Trigger>
                                    <Trigger Property="IsEnabled" Value="False">
                                        <Setter Property="Background" Value="#BDBDBD"/>
                                        <Setter Property="Cursor" Value="Arrow"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>