<Page x:Class="Pet_Shop_Project.Views.UserManagement"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      mc:Ignorable="d" 
      d:DesignHeight="650" d:DesignWidth="940"
      Background="#F5F5F5">

    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal">
                <materialDesign:PackIcon Kind="AccountGroup"
                                       Width="36" Height="36"
                                       Foreground="#FF6B6B"
                                       VerticalAlignment="Center"
                                       Margin="0,0,12,0"/>
                <TextBlock Text="QUẢN LÝ NGƯỜI DÙNG"
                          FontSize="28"
                          FontWeight="Bold"
                          Foreground="#333"
                          VerticalAlignment="Center"/>
            </StackPanel>

            <!-- Add User Button -->
            <Button x:Name="AddUserButton" Grid.Column="1"
                   Content="Thêm người dùng"
                   Height="45"
                   Padding="20,0"
                   FontSize="14"
                   FontWeight="SemiBold"
                   Background="#4CAF50"
                   Foreground="White"
                   BorderThickness="0"
                   Cursor="Hand"
                   Click="AddUserButton_Click">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                               CornerRadius="22"
                               Padding="{TemplateBinding Padding}">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <materialDesign:PackIcon Kind="AccountPlus"
                                                       Width="20" Height="20"
                                                       Foreground="White"
                                                       VerticalAlignment="Center"
                                                       Margin="0,0,8,0"/>
                                <ContentPresenter VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </Grid>

        <!-- Search and Filter -->
        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="200"/>
            </Grid.ColumnDefinitions>

            <!-- Search Box -->
            <Border Grid.Column="0"
                   Background="White"
                   CornerRadius="12"
                   Height="45"
                   Padding="15,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <materialDesign:PackIcon Grid.Column="0"
                                           Kind="Magnify"
                                           Width="22" Height="22"
                                           Foreground="#999"
                                           VerticalAlignment="Center"
                                           Margin="0,0,10,0"/>

                    <TextBox Grid.Column="1"
                            x:Name="SearchTextBox"
                            FontSize="14"
                            Background="Transparent"
                            BorderThickness="0"
                            VerticalAlignment="Center"
                            Foreground="#333"
                            TextChanged="SearchTextBox_TextChanged"
                            materialDesign:HintAssist.Hint="Tìm kiếm theo tên, email, số điện thoại..."
                            materialDesign:HintAssist.Foreground="#999"/>

                    <Button Grid.Column="2"
                           Content="Tìm"
                           Width="70"
                           Height="32"
                           Background="#FF6B6B"
                           Foreground="White"
                           BorderThickness="0"
                           FontSize="13"
                           FontWeight="SemiBold"
                           Cursor="Hand"
                           Click="SearchButton_Click">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}"
                                       CornerRadius="8">
                                    <ContentPresenter HorizontalAlignment="Center"
                                                    VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </Grid>
            </Border>

            <!-- Role Filter -->
            <Border Grid.Column="2"
                   Background="White"
                   CornerRadius="12"
                   Padding="15,0">
                <ComboBox x:Name="RoleFilterComboBox"
                         FontSize="14"
                         BorderThickness="0"
                         Background="Transparent"
                         SelectionChanged="RoleFilterComboBox_SelectionChanged">
                    <ComboBoxItem Content="Tất cả vai trò" IsSelected="True"/>
                    <ComboBoxItem Content="Quản trị viên"/>
                    <ComboBoxItem Content="Khách hàng"/>
                </ComboBox>
            </Border>
        </Grid>

        <!-- User List -->
        <Border Grid.Row="2"
               Background="White"
               CornerRadius="15"
               Padding="0">
            <Border.Effect>
                <DropShadowEffect ShadowDepth="0" BlurRadius="20" Opacity="0.1"/>
            </Border.Effect>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Table Header -->
                <Border Grid.Row="0"
                       Background="#F8FFFE"
                       CornerRadius="15,15,0,0"
                       Padding="25,15">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="80"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="200"/>
                            <ColumnDefinition Width="130"/>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="120"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" Text="ID" FontWeight="Bold" FontSize="13" Foreground="#666" VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="1" Text="Họ và Tên" FontWeight="Bold" FontSize="13" Foreground="#666" VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="2" Text="Email" FontWeight="Bold" FontSize="13" Foreground="#666" VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="3" Text="Số điện thoại" FontWeight="Bold" FontSize="13" Foreground="#666" VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="4" Text="Vai trò" FontWeight="Bold" FontSize="13" Foreground="#666" VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="5" Text="Thao tác" FontWeight="Bold" FontSize="13" Foreground="#666" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Grid>
                </Border>

                <!-- User List ScrollViewer -->
                <ScrollViewer Grid.Row="1"
                             VerticalScrollBarVisibility="Auto"
                             HorizontalScrollBarVisibility="Disabled"
                             Padding="0">
                    <ItemsControl x:Name="UserListItemsControl">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="White"
                                       BorderBrush="#F0F0F0"
                                       BorderThickness="0,0,0,1"
                                       Padding="25,18"
                                       MouseEnter="UserItem_MouseEnter"
                                       MouseLeave="UserItem_MouseLeave">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="80"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="200"/>
                                            <ColumnDefinition Width="130"/>
                                            <ColumnDefinition Width="120"/>
                                            <ColumnDefinition Width="120"/>
                                        </Grid.ColumnDefinitions>

                                        <!-- User ID -->
                                        <TextBlock Grid.Column="0" 
                                                  Text="{Binding UserId}" 
                                                  FontSize="13" 
                                                  Foreground="#666"
                                                  VerticalAlignment="Center"
                                                  FontWeight="Medium"/>

                                        <!-- Full Name with Icon -->
                                        <StackPanel Grid.Column="1" 
                                                   Orientation="Horizontal" 
                                                   VerticalAlignment="Center">
                                            <Border Background="#E3F2FD"
                                                   CornerRadius="20"
                                                   Width="36"
                                                   Height="36"
                                                   Margin="0,0,12,0">
                                                <materialDesign:PackIcon Kind="Account"
                                                                       Width="20" Height="20"
                                                                       Foreground="#2196F3"
                                                                       HorizontalAlignment="Center"
                                                                       VerticalAlignment="Center"/>
                                            </Border>
                                            <TextBlock Text="{Binding FullName}" 
                                                      FontSize="14" 
                                                      Foreground="#333"
                                                      FontWeight="SemiBold"
                                                      VerticalAlignment="Center"
                                                      TextTrimming="CharacterEllipsis"/>
                                        </StackPanel>

                                        <!-- Email -->
                                        <TextBlock Grid.Column="2" 
                                                  Text="{Binding Email}" 
                                                  FontSize="13" 
                                                  Foreground="#666"
                                                  VerticalAlignment="Center"
                                                  TextTrimming="CharacterEllipsis"/>

                                        <!-- Phone -->
                                        <TextBlock Grid.Column="3" 
                                                  Text="{Binding Phone}" 
                                                  FontSize="13" 
                                                  Foreground="#666"
                                                  VerticalAlignment="Center"/>

                                        <!-- Role Badge -->
                                        <Border Grid.Column="4"
                                               Background="{Binding RoleColor}"
                                               CornerRadius="15"
                                               Padding="12,6"
                                               HorizontalAlignment="Left"
                                               VerticalAlignment="Center">
                                            <TextBlock Text="{Binding RoleDisplay}" 
                                                      FontSize="12" 
                                                      FontWeight="SemiBold"
                                                      Foreground="White"/>
                                        </Border>

                                        <!-- Action Buttons -->
                                        <StackPanel Grid.Column="5" 
                                                   Orientation="Horizontal"
                                                   HorizontalAlignment="Center"
                                                   VerticalAlignment="Center">
                                            <!-- View Detail Button -->
                                            <Button Width="36" Height="36"
                                                   Background="#E8F5E9"
                                                   BorderThickness="0"
                                                   Cursor="Hand"
                                                   ToolTip="Xem chi tiết"
                                                   Click="ViewUserButton_Click"
                                                   Tag="{Binding UserId}" 
                                                   Margin="0,0,5,0">
                                                <Button.Template>
                                                    <ControlTemplate TargetType="Button">
                                                        <Border Background="{TemplateBinding Background}"
                                                               CornerRadius="8">
                                                            <materialDesign:PackIcon Kind="Eye"
                                                                                   Width="18" Height="18"
                                                                                   Foreground="#4CAF50"
                                                                                   HorizontalAlignment="Center"
                                                                                   VerticalAlignment="Center"/>
                                                        </Border>
                                                    </ControlTemplate>
                                                </Button.Template>
                                            </Button>

                                            <!-- Edit Button -->
                                            <Button Width="36" Height="36"
                                                   Background="#E3F2FD"
                                                   BorderThickness="0"
                                                   Cursor="Hand"
                                                   ToolTip="Chỉnh sửa"
                                                   Click="EditUserButton_Click"
                                                   Tag="{Binding UserId}" 
                                                   Margin="0,0,5,0">
                                                <Button.Template>
                                                    <ControlTemplate TargetType="Button">
                                                        <Border Background="{TemplateBinding Background}"
                                                               CornerRadius="8">
                                                            <materialDesign:PackIcon Kind="Pencil"
                                                                                   Width="18" Height="18"
                                                                                   Foreground="#2196F3"
                                                                                   HorizontalAlignment="Center"
                                                                                   VerticalAlignment="Center"/>
                                                        </Border>
                                                    </ControlTemplate>
                                                </Button.Template>
                                            </Button>

                                            <!-- Delete Button -->
                                            <Button Width="36" Height="36"
                                                   Background="#FFEBEE"
                                                   BorderThickness="0"
                                                   Cursor="Hand"
                                                   ToolTip="Xóa"
                                                   Click="DeleteUserButton_Click"
                                                   Tag="{Binding UserId}">
                                                <Button.Template>
                                                    <ControlTemplate TargetType="Button">
                                                        <Border Background="{TemplateBinding Background}"
                                                               CornerRadius="8">
                                                            <materialDesign:PackIcon Kind="Delete"
                                                                                   Width="18" Height="18"
                                                                                   Foreground="#F44336"
                                                                                   HorizontalAlignment="Center"
                                                                                   VerticalAlignment="Center"/>
                                                        </Border>
                                                    </ControlTemplate>
                                                </Button.Template>
                                            </Button>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>

                <!-- Empty State -->
                <StackPanel Grid.Row="1"
                           x:Name="EmptyStatePanel"
                           Visibility="Collapsed"
                           VerticalAlignment="Center"
                           HorizontalAlignment="Center">
                    <materialDesign:PackIcon Kind="AccountOff"
                                           Width="80" Height="80"
                                           Foreground="#CCC"
                                           HorizontalAlignment="Center"
                                           Margin="0,0,0,15"/>
                    <TextBlock Text="Không tìm thấy người dùng"
                              FontSize="18"
                              Foreground="#999"
                              HorizontalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Page>