<Page x:Class="Pet_Shop_Project.Views.SignUp"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes" Height="368">

    <ScrollViewer VerticalScrollBarVisibility="Auto" Width="353" Margin="0,-28,0,0">
        <StackPanel Margin="30" MaxWidth="400" Width="271" Height="701">
            <!-- Logo -->
            <Image Source="/Images/peach.png"
                   Width="60"
                   Height="60"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,10"/>

            <!-- Title -->
            <TextBlock Text="Tạo tài khoản mới" 
                       FontSize="25" 
                       FontWeight="Bold" 
                       HorizontalAlignment="Center" 
                       Margin="0,0,0,20"
                       Foreground="{DynamicResource MaterialDesignBody}"/>

            <!-- Full Name -->
            <TextBox x:Name="FullNameTextBox"
                     materialDesign:HintAssist.Hint="👤 Họ và tên"
                     Style="{StaticResource MaterialDesignOutlinedTextBox}"
                     Margin="0,0,0,15"
                     FontSize="14"/>

            <!-- Username -->
            <TextBox x:Name="UsernameTextBox"
                     materialDesign:HintAssist.Hint="✨ Tên đăng nhập"
                     Style="{StaticResource MaterialDesignOutlinedTextBox}"
                     Margin="0,0,0,15"
                     FontSize="14"/>

            <!-- Email -->
            <TextBox x:Name="EmailTextBox"
                     materialDesign:HintAssist.Hint="📧 Email"
                     Style="{StaticResource MaterialDesignOutlinedTextBox}"
                     Margin="0,0,0,15"
                     FontSize="14"/>

            <!-- Phone Number -->
            <TextBox x:Name="PhoneTextBox"
                     materialDesign:HintAssist.Hint="📱 Số điện thoại"
                     Style="{StaticResource MaterialDesignOutlinedTextBox}"
                     Margin="0,0,0,15"
                     FontSize="14"/>

            <!-- Password -->
            <PasswordBox x:Name="PasswordBox"
                         materialDesign:HintAssist.Hint="🔒 Mật khẩu"
                         Style="{StaticResource MaterialDesignOutlinedPasswordBox}"
                         Margin="0,0,0,15"
                         FontSize="14"/>

            <!-- Confirm Password -->
            <PasswordBox x:Name="ConfirmPasswordBox"
                         materialDesign:HintAssist.Hint="🔒 Xác nhận mật khẩu"
                         Style="{StaticResource MaterialDesignOutlinedPasswordBox}"
                         Margin="0,0,0,15"
                         FontSize="14"
                         KeyDown="ConfirmPasswordBox_KeyDown"/>

            <!-- Terms and Conditions -->
            <CheckBox x:Name="AgreeTermsCheckBox"
                      Margin="0,10,0,20"
                      FontSize="13">
                <TextBlock TextWrapping="Wrap">
                    <Run Text="Tôi đồng ý với mọi "/>
                    <Hyperlink Click="Terms_Click">Điều kiện và điều khoản</Hyperlink>
                </TextBlock>
            </CheckBox>

            <!-- Error Message -->
            <TextBlock x:Name="ErrorMessageTextBlock"
                       Foreground="Red"
                       FontSize="12"
                       Margin="0,0,0,10"
                       TextWrapping="Wrap"
                       HorizontalAlignment="Center"
                       Visibility="Collapsed"/>

            <!-- Sign Up Button -->
            <Button x:Name="SignUpButton"
                    Content="ĐĂNG KÝ"
                    Height="45"
                    FontSize="14"
                    Style="{StaticResource GradientButtonStyle}"
                    materialDesign:ButtonAssist.CornerRadius="5"
                    Click="SignUp_Click"
                    Margin="0,0,0,6"/>

            <!-- Already have account -->
            <StackPanel Orientation="Horizontal" 
                        HorizontalAlignment="Center"
                        Margin="0,10,0,0">
                <TextBlock Text="Đã có tài khoản? " 
                           FontSize="14"
                           VerticalAlignment="Center"/>
                <Button Content="Đăng nhập?"
                        Style="{StaticResource MaterialDesignFlatButton}"
                        Click="SignIn_Click"
                        FontStyle="Italic"
                        FontSize="14"
                        Padding="5,0"/>
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</Page>